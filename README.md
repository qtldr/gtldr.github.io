# 🚀 qTLDR

## Start developing.
Navigate into directory and start it up.
```shell
npm run develop
```

## Deploy to gh-pages
Need to ensure repository settings has GitHub pages using ```gh-pages``` branch as source
```shell
npm run deploy 
```